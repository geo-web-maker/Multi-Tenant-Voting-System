# frontend/.env
VITE_API_URL=http://127.0.0.1:8000
VITE_ORG_SLUG=kyuccu
# Shown instantly on the boot splash, before any network call resolves.
# Set these per-org in each Vercel project's env settings (not committed values here).
VITE_ELECTION_NAME=
VITE_LOGO_URL=